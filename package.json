{
  "name": "coffee-machine",
  "devDependencies": {
    "mocha": "^2.3.4",
    "mocha-junit-reporter": "^1.9.1"
  },
  "scripts": {
    "test": "mocha test --recursive --reporter mocha-junit-reporter"
  }
}
